# ==================================
# CONFIGURAÇÕES DO MYSQL (Container)
# ==================================
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=meu_db

# NÃO USE MYSQL_USER=root!
# O usuário root já é criado automaticamente
# MYSQL_USER e MYSQL_PASSWORD são para criar um usuário ADICIONAL

# ==================================
# CONFIGURAÇÕES DA APLICAÇÃO PHP
# ==================================
# Para PHP LOCAL (php -S):
DB_HOST=127.0.0.1
DB_PORT=3308

# Para PHP no DOCKER (descomente as linhas abaixo):
# DB_HOST=mysql
# DB_PORT=3306

# Comum para ambos:
DB_NAME=meu_db
DB_USER=root
DB_PASSWORD=root

# ==================================
# NOTAS IMPORTANTES:
# ==================================
# 1. Este arquivo deve estar na RAIZ do projeto, não em workflow/
# 2. O usuário root já existe, não precisa criar com MYSQL_USER
# 3. Para rodar PHP LOCAL: use DB_HOST=127.0.0.1 e DB_PORT=3308
# 4. Para rodar PHP no DOCKER: use DB_HOST=mysql e DB_PORT=3306
# 5. A porta 3308 é onde o MySQL do Docker está exposto no seu localhost